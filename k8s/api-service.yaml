apiVersion: v1
kind: Service
metadata:
  name: oficina-mecanica-api
  namespace: oficina-mecanica
spec:
  type: LoadBalancer
  selector:
    app: oficina-mecanica-api
  ports:
    - name: http
      port: 18080
      targetPort: 8080
